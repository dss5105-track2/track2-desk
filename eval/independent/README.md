# 独立分类任务（全员 · 周二 23:59 前交）

## 为什么要做

我们要知道：一个认真的人只看数据，会把这 30 条群聊请求判成什么。六个人的**分歧**会告诉我们哪些请求本身有歧义，这是标准答案可信的依据，也会写进 Evaluation.pdf。

所以这份作业没有标准答案可对，判错不扣分；**抄了、商量了、问了 AI，这份数据就作废。**

## 规则

1. **独立完成。** 交之前不和组员讨论，不用 ChatGPT、Claude 或任何 AI。
2. **只看这几个文件：**
   - `docs/Factory_Primer.md`：三分钟行业入门
   - `data/data_dictionary.md`：字段说明
   - `data/dispatch_requests.txt`：30 条请求原文
   - `data/orders.csv`：120 张订单，可以用 Excel 打开筛选
   - `data/workshops.csv`：8 家车间
   - 本文件
3. **交之前不要打开**：`eval/` 下除本文件夹外的所有文件、`language/`、`docs/results_week1.md`、`docs/week1_status.md`、项目书。这些里面有答案线索。
4. **用 Excel 打开 `分类模板.csv`，填好后另存为 `分类_你的名字.csv`，私发给张锦若，不要发群里。**

## 背景设定

- 今天是 **2026-04-01** 早上。所有请求都是今天发出的外发请求。
- 请求按时间顺序到达。前面某条消息里说的"本周都别给某车间"这类话，对后面的请求也有效。
- 每家车间能不能接，看三件事：能做这个品类吗（`makes`），允许接吗（`status`），批量是否超过上限（`max_batch_pieces`）。
- 所有车间都至少要 1 天取送，今天到期的单子不可能今天拿回来。

## 要填的列

| 列 | 怎么填 |
|---|---|
| 官方类别 | 必填，四选一：`extract`、`clarify`、`refuse`、`decline` |
| 内部子类 | 选填，见下表，拿不准可以空着 |
| 依据 | 必填，一句话，尽量引用数据，例如"orders.csv 里 X 客户有 3 单在途" |
| 把握 | 必填，三选一：`高`、`中`、`低` |
| 填写人 | 你的名字 |

## 四个官方类别

| 类别 | 意思 | 内部子类 |
|---|---|---|
| **extract** | 信息完整、要求合法，系统应该提取信息并给出派单方案 | `allocate`：有按时方案，直接推荐<br>`no_on_time`：信息完整，但任何车间都来不及，要说明晚几天并给选项 |
| **clarify** | 系统不能直接派单，应该先问人 | `ambiguous`：指代不清，数据里对应多张订单<br>`missing`：缺派单必需的信息<br>`state_conflict`：订单状态有矛盾，例如已经快做完了 |
| **refuse** | 请求指定了一家不能接这单的车间，系统应拒绝并说明原因、给替代 | `ineligible` |
| **decline** | 数据里根本没有答案，系统应说"无法回答"，不能编 | `not_in_data` |

注意区分：请求里说"别给某车间"是**用户约束**，系统照做即可，不算 refuse。只有"指定要给某车间、而那家不能接"才是 refuse。

## 判定顺序

每条请求按顺序问下去，第一个"是"就是答案：

1. 问的是数据里根本没有的东西吗？比如历史记录、价格、不存在的订单 → `decline`
2. 能唯一确定是哪张订单吗？不能 → `clarify / ambiguous`
3. 这张订单的状态和"要外发"有矛盾吗？ → `clarify / state_conflict`
4. 派单需要的信息缺吗，且订单表里也补不上？ → `clarify / missing`
5. 指定了一家不能接的车间吗？ → `refuse`
6. 以上都不是：粗算一下有没有车间来得及？有 → `extract / allocate`；没有 → `extract / no_on_time`

第 6 步只需要粗算：`队列天数 + 件数 ÷ 日产能 + 取送天数`，看能不能在交期前完成。拿不准就在"把握"里填 `低`，不用算得很精确。

## 预计用时

读材料 20 分钟，分类 60 到 90 分钟。
