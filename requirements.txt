# Kernel, harness and tests use only the Python standard library (Python >= 3.9).

# Language layer: Claude API and local .env loading
anthropic
python-dotenv

# Optional, for a nicer test runner
pytest>=7
