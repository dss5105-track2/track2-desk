# Kernel, harness and tests use only the Python standard library (Python >= 3.9).

# Language layer: OpenAI Responses API and local .env loading
openai
python-dotenv

# Dispatcher interface: streamlit run app/streamlit_app.py
streamlit>=1.40
pandas

# Optional, for a nicer test runner
pytest>=7
